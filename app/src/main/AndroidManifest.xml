<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Smartspacer_cricket">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<provider
    android:authorities="${applicationId}.target"
    android:name=".Target"
    android:exported="true"
    android:permission="com.kieronquinn.app.smartspacer.permission.ACCESS_SMARTSPACER_TARGETS">
        <intent-filter>
            <action android:name="com.kieronquinn.app.smartspacer.TARGET" />
        </intent-filter>
</provider>
        <receiver
            android:name=".CricketAlarmReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="com.meilluer.smartspacer_cricket.action.DAILY_CHECK" />
                <action android:name="com.meilluer.smartspacer_cricket.action.PREMATCH_CHECK" />
                <action android:name="com.meilluer.smartspacer_cricket.action.LIVE_POLL" />
                <action android:name="com.meilluer.smartspacer_cricket.action.SET_DISMISS_FLAG" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
